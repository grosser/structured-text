<%= @document.title %>

<% @document.sections.each do |section| %>
  <%= render 'sections/form', :section => section %>
<% end %>

<%= link_to 'Add new section', :url => sections_path(:document_id => @document.id), :method => :post, :remote => true %>